#include <iostream> 
using namespace std;
int main() 
{
	char input;
	do 
	{
		cout << "Would you like to close the console (Y/N)?" << endl;
		cin >> input;
	} 
	while ((input != 'Y') && (input != 'y'));

	return 0;
}
