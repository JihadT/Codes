#include <iostream>
#include <string>
using namespace std;
int main()
{
	int sum = 0;

	for (int x = 0; x <= 100; x++)
	{
		if (x % 9 == 0)
		{
			cout << "\nNumber || " << x << endl;
			sum = sum + x;
		}
	}
	cout << "\nThe sum is " << sum << endl;
}
